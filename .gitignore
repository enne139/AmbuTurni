# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
*.g.dart
# pubspec.lock è VERSIONATO (raccomandazione Flutter per le app, non librerie):
# la CI e le altre macchine risolvono le stesse identiche versioni testate qui.

# Android (generato da flutter build — non versionato)
android/.gradle/
android/local.properties
android/key.properties
android/app/google-services.json

# iOS (non usato, piattaforma non abilitata)
ios/

# Local databases
*.db
*.db-shm
*.db-wal

# Environment
.env
.env.local

# macOS
.DS_Store

# IDE
.idea/
.vscode/

# Build artifacts
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Backend Node.js
backend/node_modules/
backend/.env
